<%= form_with(model: artille) do |form| %>
  <% if artille.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(artille.errors.count, "error") %> prohibited this artille from being saved:</h2>

      <ul>
        <% artille.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_area :text %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
